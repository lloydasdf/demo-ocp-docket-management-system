"use client";
import { FileText } from "lucide-react";
export function DocxPreview() { return <div className="max-w-md text-center"><FileText className="mx-auto mb-3 h-12 w-12 text-muted-foreground" /><p className="font-medium">DOCX preview is unavailable in this runtime</p><p className="mt-1 text-sm text-muted-foreground">Download the document to view it. In-browser document conversion can be added when the DOCX rendering dependency is available.</p></div>; }
